# Core dependencies
flask==3.0.0
gunicorn==21.2.0
requests==2.31.0
python-dotenv==1.2.1

# Machine Learning (with version constraints for Windows compatibility)
scikit-learn==1.3.2
numpy==1.26.2
joblib==1.3.2

# Python analysis tools
flake8==7.0.0
pylint==3.0.3
autopep8==2.0.4
astunparse==1.6.3
radon==6.0.1

# Security scanning
bandit==1.7.5

# Code complexity and maintainability
mccabe==0.7.0

# Dependency analysis
safety==3.0.1

# Testing
pytest==7.4.3

# Note: pandas excluded due to pyarrow build issues on Windows
# Note: streamlit excluded (heavy dependency, optional UI)
# For pandas support, install separately: pip install pandas --no-deps
# Docker deployment includes all dependencies without build issues

# Multi-language support (optional linters - install via system package managers)
# JavaScript/TypeScript: npm install -g eslint
# Java: apt-get install checkstyle / brew install checkstyle
# C/C++: apt-get install cppcheck / brew install cppcheck
# Go: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
# Rust: rustup component add clippy
# Ruby: gem install rubocop
# PHP: composer global require squizlabs/php_codesniffer

