# Minimal dependencies for Vercel serverless deployment
# Optimized to stay under 250MB unzipped limit

# Core web framework (minimal versions)
flask==3.0.0

# Utilities
requests==2.31.0

# Basic Python code analysis
astunparse==1.6.3

# Note: The following are excluded for serverless deployment to reduce size:
# - scikit-learn, pandas, joblib (ML features) - 150+ MB
# - pylint, flake8 (heavy linters) - 50+ MB  
# - streamlit (not compatible with serverless) - 40+ MB
# - pytest, gunicorn (dev/container only)
#
# For full functionality with ML and all linters, use Docker/GHCR deployment
# Container deployment has no size limits and includes all features

